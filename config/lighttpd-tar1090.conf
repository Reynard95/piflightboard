# DEPRECATED — no longer installed by deploy.sh or install.sh.
# Replaced by config/lighttpd-flightboard.conf (50-flightboard.conf).
#
# This file previously defined /tar1090/airline_logos/ and /tar1090/country_flags/
# aliases that piggy-backed on tar1090's 88-tar1090.conf catch-all.
# The new config owns the full web root and all aliases independently.

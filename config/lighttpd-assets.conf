# Flighttracker lighttpd config — managed by deploy script
# Alias overrides for local asset images.
# NOTE: /tar1090/airline_logos/ and /tar1090/country_flags/ are already
# defined by the tar1090 system config. They are intentionally omitted
# here to avoid duplicate-key warnings in lighttpd.
# The paths in the system config already point to /opt/flighttracker/images/.
